## User guide

This section covers how to install Kiln, use the UI, export env bundles, and run Genesis-backed simulations.

Start here:

- **Installation**: set up a conda env or pip install.
- **UI workflow**: author and preview USD.
- **Env bundles**: export `scene.usd(a)` + `env.json` and load at runtime.


