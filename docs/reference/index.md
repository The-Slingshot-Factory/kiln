## API reference

The pages in this section are generated from Kiln’s docstrings using `mkdocstrings`.

For GUI/backend integrations, prefer the stable import surface in `kiln.api`.

If a symbol is missing or unclear, improve the docstring in the corresponding Python file.


